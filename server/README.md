# KMJ ERP - Server

Quick start:

Install dependencies and run dev server:

```
cd server
npm install
npm run dev
```

Build for production:

```
npm run build
npm start
```

Configure `MONGO_URI` and `JWT_SECRET` in `.env` (see `.env.example`).
